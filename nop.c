#include "monty.h"

/**
 *nop - do nothing
 *@stack: struct with the element of the stack
 *@line_number: to push in the stack
 *Return: void
 */

void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
